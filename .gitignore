# macOS
.DS_Store

# Gradle files
.gradle/
build/

# 打包输出（assemble 后复制带时间戳的 APK，见 app/build.gradle.kts）
dist-apk/

# Local configuration file (sdk path, etc)
local.properties
/.idea/

# Editor (个人/本机设置，不纳入版本库；团队若需共享可改为提交并从此处移除)
.vscode/
.claude/

# Environment configuration (contains sensitive credentials)
env.properties

# Python virtual environment
server/venv/
/.kotlin/
PROJECT_STATE.md
